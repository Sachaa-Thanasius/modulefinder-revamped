# modulefinder-revamped
An attempt at redoing Python's modulefinder module in the standard library, for fun.

The library uses import hooks to simplify some of the package and module traversal.

(This accidentally mirrors some ideas from https://github.com/python/cpython/pull/11787.)
